package mis055.assignment.exception

import org.springframework.http.HttpStatus
import org.zalando.problem.AbstractThrowableProblem
import org.zalando.problem.StatusType
import java.net.URI

//class GenericException(type: String, title: String, httpStatus: StatusType, detail: String?
//
//): AbstractThrowableProblem(URI.create(type), title, httpStatus, detail) {
//}